export interface Attachment {
    filename: string
    path: string
  }
  
export interface EmailBody {
    name: string
    surname: string
    email: string
    phone: string
    location: string
    positionName: string
    positionId: string
  }